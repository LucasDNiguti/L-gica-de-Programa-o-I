#include <stdio.h>
#include <stdlib.h>

int main () {

    int n;

    while (n < 10)
    {
        n++;
        printf("\nValor: %d", n);
    }

    return 0;
}

// Condição no início, não há garantia de que o código será executado